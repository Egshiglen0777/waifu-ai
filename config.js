const CONFIG = {
    API_URL: "https://your-railway-backend-url.com/api/chat", // Replace with your actual Railway backend URL
    DEFAULT_WAIFU: "makima", // Default waifu selection if none is picked
};

export default CONFIG;
